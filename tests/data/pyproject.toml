[project]
name = "generate-test-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.3.2",
    "pyarrow>=21.0.0",
]

[dependency-groups]
dev = [
    "ipykernel>=6.30.1",
]
